{
  "name": "The-Genius-Lounge",
  "version": "0.1.0",
  "private": true,
  "proxy": "http://localhost:3001",
  "description": "updaate",
  "main": "index.js",
  "repository": "https://github.com/quietwar/HgpApp",
  "author": "prof.toure <hodari@hiddengeniusproject.org>",
  "license": "MIT",
  "dependencies": {
    "jquery": "^3.3.1",
    "jquery-datetimepicker": "^2.5.21",
    "react_ujs": "^2.5.0"
  },
  "scripts": {
    "build": "cd client && yarn install && yarn run build && cd ..",
    "deploy": "cp -a client/build/. public/",
    "postinstall": "yarn run build && npm run deploy && echo 'Client built!'"
  }
}
