<%= form_with(model: @admins, local: true) do |form| %>
  <div class="form-inputs">

    <%= text_field :notes, id: :notes %>
    <%= date_field :date_range, input_html: { class: "form-control input-sm date-range-picker" } %>
    <%= hidden_field :start, as: :hidden, value: Date.today, class: 'form-control input-sm start_hidden' %>
    <%= hidden_field :end, as: :hidden, value: Date.today, class: 'form-control input-sm end_hidden' %>
  </div>

    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this Genius from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>


     
<% end %>
